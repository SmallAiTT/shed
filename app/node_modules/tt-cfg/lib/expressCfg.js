var path = require("path");
var appDir = path.join(__dirname, "../../../");

module.exports = {
    appDir : appDir,
    port : 3000,
    staticDir : path.join(appDir, "public"),

    setOpt : {
        "views" : path.join(appDir, "views-jade"),
        "view engine" : "jade"
    }
}